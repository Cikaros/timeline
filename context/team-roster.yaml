# 团队名册（context/team-roster.yaml）
# 由 specflow init 生成。vars.yaml 解析器支持两层读取（全局 ~/.codex-plugin/vars.yaml
# 与项目 .codex-plugin/vars.yaml）；本文件可作为团队基线的录入模板。
# 被  变量引用（如 ）。

sprint:
  current: ""
  ends_at: ""

members:
  - id: ""
    name: ""
    role: ""
    agent: ""

review:
  required_roles: [""]
  min_approvers: 
  escalation: ""

conventions:
  working_hours: ""
  standup: ""
